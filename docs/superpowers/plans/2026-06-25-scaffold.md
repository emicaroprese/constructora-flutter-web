# Scaffold — constructora_flutter_web — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Crear el proyecto Flutter base con estructura de carpetas, dependencias instaladas, tema configurado y archivos placeholder que compilen y corran en Chrome desde el primer momento.

**Architecture:** App Flutter Web de una pantalla. Tema centralizado con Colors.grey. Navegación con go_router. Credenciales en assets/.env via flutter_dotenv. Supabase NO se inicializa todavía — eso es tarea de la sesión de conexión a base de datos.

**Tech Stack:** Flutter 3.44.3, Dart, supabase_flutter, go_router, flutter_dotenv

## Global Constraints

- Flutter 3.44.3, channel stable, solo web (Chrome)
- Paleta exclusivamente Colors.grey — nunca hex hardcodeados
- Sin modo oscuro — solo ThemeData light
- Un widget por archivo: snake_case archivos, PascalCase clases (deben coincidir)
- Lógica de Supabase solo en lib/services/ — nunca en widgets
- Credenciales Supabase en assets/.env — nunca en el código
- No usar Card, InkWell, BoxShadow ni fuentes custom
- Líneas máximo 80 caracteres, funciones menos de 20 líneas

---

### Task 1: Crear proyecto Flutter, estructura de carpetas y git

**Files:**
- Create (via flutter create): `lib/main.dart`, `pubspec.yaml`, `analysis_options.yaml`, `web/`, `test/`
- Create: `lib/screens/`, `lib/widgets/`, `lib/services/`, `lib/models/`, `assets/`
- Create: `assets/.env`
- Modify: `.gitignore`

**Interfaces:**
- Produces: proyecto Flutter compilable con estructura de carpetas y repo git inicializado

- [ ] **Step 1: Ejecutar flutter create en la carpeta actual**

```bash
flutter create --project-name constructora_flutter_web .
```

Esperado: Flutter genera los archivos base. Los archivos .md y .claude/ existentes no se modifican.

- [ ] **Step 2: Crear las carpetas de lib/ y assets/**

```bash
mkdir -p lib/screens lib/widgets lib/services lib/models assets
```

- [ ] **Step 3: Crear assets/.env con placeholders**

Crear `assets/.env`:
```
SUPABASE_URL=
SUPABASE_ANON_KEY=
```

- [ ] **Step 4: Agregar assets/.env al .gitignore**

Agregar al final del `.gitignore` generado por Flutter:
```
# Supabase credentials
assets/.env
```

- [ ] **Step 5: Inicializar repositorio git**

```bash
git init
git add -A
git commit -m "chore: scaffold inicial Flutter Web"
```

Esperado: commit creado con todos los archivos de Flutter.

- [ ] **Step 6: Verificar que el proyecto corre**

```bash
flutter run -d chrome
```

Esperado: app corriendo en Chrome mostrando el contador default de Flutter (lo reemplazamos en Task 3).

---

### Task 2: Agregar dependencias

**Files:**
- Modify: `pubspec.yaml`

**Interfaces:**
- Consumes: proyecto Flutter base de Task 1
- Produces: pubspec.yaml con supabase_flutter, go_router y flutter_dotenv; assets/.env declarado

- [ ] **Step 1: Agregar dependencias vía pub**

```bash
flutter pub add supabase_flutter go_router flutter_dotenv
```

Esperado: pubspec.yaml actualizado con las tres dependencias.

- [ ] **Step 2: Declarar assets/.env en pubspec.yaml**

En `pubspec.yaml`, dentro de la sección `flutter:`, agregar la declaración de assets:

```yaml
flutter:
  uses-material-design: true
  assets:
    - assets/.env
```

- [ ] **Step 3: Instalar dependencias**

```bash
flutter pub get
```

Esperado: sin errores. pubspec.lock actualizado.

- [ ] **Step 4: Verificar análisis estático**

```bash
dart analyze
```

Esperado: `No issues found!`

- [ ] **Step 5: Commit**

```bash
git add pubspec.yaml pubspec.lock
git commit -m "chore: agregar dependencias supabase_flutter go_router flutter_dotenv"
```

---

### Task 3: Configurar main.dart, tema y pantalla placeholder

**Files:**
- Modify: `lib/main.dart`
- Create: `lib/screens/dashboard_screen.dart`
- Delete: `test/widget_test.dart` (test del contador default — ya no aplica)

**Interfaces:**
- Consumes: dependencias de Task 2
- Produces: app corriendo con tema gris, AppBar transparente, go_router → DashboardScreen

- [ ] **Step 1: Eliminar el test default de Flutter**

```bash
rm test/widget_test.dart
```

- [ ] **Step 2: Crear lib/screens/dashboard_screen.dart**

```dart
import 'package:flutter/material.dart';

class DashboardScreen extends StatelessWidget {
  const DashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Dashboard'),
      ),
    );
  }
}
```

- [ ] **Step 3: Reescribir lib/main.dart**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_dotenv/flutter_dotenv.dart';
import 'package:go_router/go_router.dart';
import 'screens/dashboard_screen.dart';

Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await dotenv.load(fileName: 'assets/.env');
  runApp(const App());
}

final _router = GoRouter(
  routes: [
    GoRoute(
      path: '/',
      builder: (context, state) => const DashboardScreen(),
    ),
  ],
);

class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp.router(
      title: 'Dashboard Personal',
      debugShowCheckedModeBanner: false,
      routerConfig: _router,
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(
          seedColor: Colors.grey,
          brightness: Brightness.light,
        ),
        appBarTheme: const AppBarTheme(
          backgroundColor: Colors.transparent,
          elevation: 0,
        ),
        scaffoldBackgroundColor: Colors.grey[50],
      ),
    );
  }
}
```

- [ ] **Step 4: Verificar análisis estático**

```bash
dart analyze
```

Esperado: `No issues found!`

- [ ] **Step 5: Verificar en Chrome**

```bash
flutter run -d chrome
```

Esperado: app en Chrome mostrando "Dashboard" centrado, AppBar transparente, fondo gris claro.

- [ ] **Step 6: Commit**

```bash
git add lib/main.dart lib/screens/dashboard_screen.dart
git commit -m "feat: configurar tema, go_router y dashboard placeholder"
```

---

### Task 4: Crear modelos Tarea e ItemCompra con TDD

**Files:**
- Create: `lib/models/tarea.dart`
- Create: `lib/models/item_compra.dart`
- Create: `test/models/tarea_test.dart`
- Create: `test/models/item_compra_test.dart`

**Interfaces:**
- Produces: `Tarea.fromJson(Map<String, dynamic> json)` → `Tarea`
- Produces: `ItemCompra.fromJson(Map<String, dynamic> json)` → `ItemCompra`

- [ ] **Step 1: Escribir test para Tarea (va a fallar)**

Crear `test/models/tarea_test.dart`:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:constructora_flutter_web/models/tarea.dart';

void main() {
  group('Tarea', () {
    test('fromJson parsea todos los campos correctamente', () {
      final json = <String, dynamic>{
        'id': '1',
        'titulo': 'Comprar leche',
        'descripcion': 'En el super',
        'completada': false,
        'fecha': '2026-06-25T00:00:00.000Z',
        'prioridad': 'alta',
      };

      final tarea = Tarea.fromJson(json);

      expect(tarea.id, '1');
      expect(tarea.titulo, 'Comprar leche');
      expect(tarea.descripcion, 'En el super');
      expect(tarea.completada, false);
      expect(tarea.prioridad, 'alta');
    });
  });
}
```

- [ ] **Step 2: Correr el test (debe fallar)**

```bash
flutter test test/models/tarea_test.dart
```

Esperado: error `Target of URI doesn't exist` porque el modelo no existe aún.

- [ ] **Step 3: Implementar lib/models/tarea.dart**

```dart
class Tarea {
  final String id;
  final String titulo;
  final String descripcion;
  final bool completada;
  final DateTime fecha;
  final String prioridad;

  const Tarea({
    required this.id,
    required this.titulo,
    required this.descripcion,
    required this.completada,
    required this.fecha,
    required this.prioridad,
  });

  factory Tarea.fromJson(Map<String, dynamic> json) => Tarea(
        id: json['id'] as String,
        titulo: json['titulo'] as String,
        descripcion: json['descripcion'] as String,
        completada: json['completada'] as bool,
        fecha: DateTime.parse(json['fecha'] as String),
        prioridad: json['prioridad'] as String,
      );
}
```

- [ ] **Step 4: Correr el test (debe pasar)**

```bash
flutter test test/models/tarea_test.dart
```

Esperado: `All tests passed!`

- [ ] **Step 5: Escribir test para ItemCompra (va a fallar)**

Crear `test/models/item_compra_test.dart`:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:constructora_flutter_web/models/item_compra.dart';

void main() {
  group('ItemCompra', () {
    test('fromJson parsea todos los campos correctamente', () {
      final json = <String, dynamic>{
        'id': '1',
        'nombre': 'Leche',
        'cantidad': 2,
        'precio': 1.50,
        'comprado': false,
      };

      final item = ItemCompra.fromJson(json);

      expect(item.id, '1');
      expect(item.nombre, 'Leche');
      expect(item.cantidad, 2);
      expect(item.precio, 1.50);
      expect(item.comprado, false);
    });
  });
}
```

- [ ] **Step 6: Implementar lib/models/item_compra.dart**

```dart
class ItemCompra {
  final String id;
  final String nombre;
  final int cantidad;
  final double precio;
  final bool comprado;

  const ItemCompra({
    required this.id,
    required this.nombre,
    required this.cantidad,
    required this.precio,
    required this.comprado,
  });

  factory ItemCompra.fromJson(Map<String, dynamic> json) => ItemCompra(
        id: json['id'] as String,
        nombre: json['nombre'] as String,
        cantidad: json['cantidad'] as int,
        precio: (json['precio'] as num).toDouble(),
        comprado: json['comprado'] as bool,
      );
}
```

- [ ] **Step 7: Correr todos los tests de modelos**

```bash
flutter test test/models/
```

Esperado: `All tests passed!`

- [ ] **Step 8: Commit**

```bash
git add lib/models/ test/models/
git commit -m "feat: agregar modelos Tarea e ItemCompra con fromJson"
```

---

### Task 5: Crear placeholders de services y widgets

**Files:**
- Create: `lib/services/tarea_service.dart`
- Create: `lib/services/compra_service.dart`
- Create: `lib/widgets/tarea_list.dart`
- Create: `lib/widgets/compra_list.dart`

**Interfaces:**
- Produces: clases vacías compilables; listos para implementar en sesión de Supabase

- [ ] **Step 1: Crear lib/services/tarea_service.dart**

```dart
class TareaService {
  // Operaciones Supabase — implementar en sesión de conexión DB
}
```

- [ ] **Step 2: Crear lib/services/compra_service.dart**

```dart
class CompraService {
  // Operaciones Supabase — implementar en sesión de conexión DB
}
```

- [ ] **Step 3: Crear lib/widgets/tarea_list.dart**

```dart
import 'package:flutter/material.dart';

class TareaList extends StatelessWidget {
  const TareaList({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
```

- [ ] **Step 4: Crear lib/widgets/compra_list.dart**

```dart
import 'package:flutter/material.dart';

class CompraList extends StatelessWidget {
  const CompraList({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
```

- [ ] **Step 5: Verificar análisis estático**

```bash
dart analyze
```

Esperado: `No issues found!`

- [ ] **Step 6: Verificar en Chrome**

```bash
flutter run -d chrome
```

Esperado: app corriendo sin errores, "Dashboard" visible.

- [ ] **Step 7: Commit final del scaffold**

```bash
git add lib/services/ lib/widgets/
git commit -m "chore: agregar placeholders de services y widgets"
```

---

## Nota: GitHub

Después de completar todos los tasks, crear el repo en GitHub y conectarlo:

```bash
git remote add origin https://github.com/<tu-usuario>/constructora-flutter-web.git
git branch -M main
git push -u origin main
```

El repo en GitHub lo crea Emi desde github.com antes de ejecutar estos comandos.
